# Core packages
nicegui
openai
python-dotenv

# Audio packages
pyaudio
sounddevice
soundfile

# Utilities
requests
tqdm
keyboard

# Optional packages - will be installed if compatible
numpy
python-socketio
python-engineio

# TTS will be installed separately with fallback options
