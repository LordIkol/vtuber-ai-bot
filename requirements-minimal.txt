# Core packages
nicegui
openai
python-dotenv

# Audio packages
pyaudio
sounddevice
soundfile
pydub

# Twitch integration
twitchio

# VTube Studio integration
websockets

# Utilities
requests
tqdm
keyboard
numpy

# TTS will be installed separately with fallback options
TTS